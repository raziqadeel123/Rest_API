let Joi,bcrypt,User,JwtService,CustomErrorHandler;_fe1‍.x([["default",()=>_fe1‍.o]]);_fe1‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_fe1‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_fe1‍.w("../../models",[["User",["User"],function(v){User=v}]]);_fe1‍.w("../../Services/JWTService",[["default",["JwtService"],function(v){JwtService=v}]]);_fe1‍.w("../../Services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);





const registerController = {
  async register(req, res, next) {
    // validation

    const registerSchema = Joi.object({
      name: Joi.string().min(3).max(30).required(),
      email: Joi.string().email().required(),
      password: Joi.string()
        .pattern(new RegExp('^[a-zA-Z0-9]{3,30}$'))
        .required(),
      repeat_password: Joi.ref('password'),
    });

    const { error } = registerSchema.validate(req.body);

    if (error) {
      return next(error);
    }

    // check if user is in the database alredy
    try {
      const existingUser = await User.exists({ email: req.body.email });
      if (existingUser) {
        return next(
          CustomErrorHandler.alreadyExist('This email is alreay taken')
        );
      }
    } catch (err) {
      return next(err);
    }

    // Hash password
    const { name, email, password } = req.body;
    const hashedPassword = await bcrypt.hash(password, 10);

    // prepare the model

    const user = new User({
      name,
      email,
      password: hashedPassword,
    });

    let access_token;
    try {
      const result = await user.save();
      _fe1‍.g.console.log(result);
      // Token
      access_token = JwtService.sign({ _id: result._id, role: result.role });
    } catch (err) {
      return next(err);
    }

    res.json({ access_token: access_token });
  },
};

_fe1‍.d(registerController);

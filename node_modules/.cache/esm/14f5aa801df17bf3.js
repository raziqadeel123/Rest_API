let Joi,JwtService,User,bcrypt,CustomErrorHandler;_1df‍.x([["default",()=>_1df‍.o]]);_1df‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_1df‍.w("../../Services/JWTService",[["default",["JwtService"],function(v){JwtService=v}]]);_1df‍.w("../../models",[["User",["User"],function(v){User=v}]]);_1df‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_1df‍.w("../../Services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);




const loginController = {
  async login(req, res, next) {
    // validation
    const LoginSchema = Joi.object({
      email: Joi.string().email().required(),
      password: Joi.string()
        .pattern(new RegExp('^[a-zA-Z0-9]{3,30}$'))
        .required(),
    });

    const { error } = LoginSchema.validate(req.body);
    if (error) {
      return next(error);
    }
    try {
      const user = await User.findOne({ email: req.body.email });
      if (!user) {
        return next(CustomErrorHandler.wrongCredentials());
      }
      // compare the password
      const match = await bcrypt.compare(req.body.password, user.password);
      if (!match) {
        return next(CustomErrorHandler.wrongCredentials());
      }

      // Token
      const access_token = JwtService.sign({
        _id: user._id,
        role: user.role,
      });

      res.json({ access_token: access_token });
    } catch (err) {
      return next(err);
    }
  },
};

_1df‍.d(loginController);
